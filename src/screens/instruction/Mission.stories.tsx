import React from 'react';
import { Meta } from '@storybook/react';

import MissionScreen from '.';

export default {
    title: 'Screens/Mission',
    component: MissionScreen,
} as Meta;

export const Primary = () => <MissionScreen />;
